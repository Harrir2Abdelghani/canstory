{
  "name": "canstory-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:web": "cd web && npm install",
    "build:web": "cd web && npm run build",
    "start:backend": "cd web && node server.js",
    "dev:backend": "cd web && node server.js"
  }
}
